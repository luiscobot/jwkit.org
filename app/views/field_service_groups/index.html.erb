<% content_for(:title) { t("field_service_group.title.plural") } %>

<section class="flex flex-col gap-8 h-full">
  <div class="flex justify-between items-start">
    <div>
      <h1 class="font-inter font-bold text-xl leading-6 text-jw-white"><%= t("field_service_group.title.plural") %></h1>
      <p class="mt-2 font-inter font-medium text-base text-jw-quick-silver"><%= t("congregation") %> Rosales</p>
    </div>

    <div>
      <%= link_to t("field_service_group.new.title"), new_field_service_group_path, class: "commit" %>
    </div>
  </div>

  <div class="flex flex-col h-full">
    <div class="grid grid-cols-3 gap-8">
      <%= render @field_service_groups %>
    </div>
  </div>
</section>
