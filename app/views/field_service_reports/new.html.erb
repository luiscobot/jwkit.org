<section class="grid grid-cols-4 gap-8">
  <h1 class="col-span-full mt-3 text-3xl font-semibold">Create Field Service Report</h1>

  <%= form_with model: @field_service_report, class: "col-span-2" do |form| %>
    <h2 class="text-xl font-medium">Publisher <%= @publisher.name %></h2>

    <div class="mt-6 grid grid-cols-2 gap-6 p-6 bg-white rounded-xl text-slate-950">
      <div class="flex flex-col gap-2 col-span-1">
        <%= form.label :month, class: label_style %>
        <%= form.select :month, month_options, {}, class: control_style %>
      </div>

      <div class="flex flex-col gap-2 col-span-1">
        <%= form.label :year, class: label_style %>
        <%= form.select :year, year_options, {}, class: control_style %>
      </div>

      <div class="flex items-center gap-2 col-span-full">
        <%= form.check_box :shared_in_ministry, class: "h-4 w-4 rounded border-jw-san-marino/25 text-jw-san-marino focus:ring-jw-san-marino" %>
        <%= form.label :shared_in_ministry, class: label_style %>
      </div>

      <div class="flex flex-col gap-2 col-span-1">
        <%= form.label :bible_studies, class: label_style %>
        <%= form.text_field :bible_studies, class: control_style %>
      </div>

      <div class="flex flex-col gap-2 col-span-1">
        <%= form.label :hours, class: label_style %>
        <%= form.text_field :hours, class: control_style %>
      </div>

      <div class="flex flex-col gap-2 col-span-full">
        <%= form.label :comments, class: label_style %>
        <%= form.text_area :comments, class: "#{control_style} resize-none" %>
      </div>
    </div>

    <%= form.hidden_field :publisher_id, value: @publisher.id %>

    <%= form.submit class: "mt-6 p-2.5 bg-jw-san-marino rounded-md font-medium text-sm text-slate-50 text-center cursor-pointer" %>
  <% end %>
</section>