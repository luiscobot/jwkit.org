<% content_for(:html_title) { t("field_service_report.title.singular") } %>

<section class="flex flex-col gap-8 h-full">
  <div>
    <h1 class="font-inter font-bold text-xl leading-6 text-jw-white"><%= t("field_service_report.title.singular") %></h1>
    <p class="mt-2 font-inter font-medium text-base text-jw-quick-silver"><%= t("publisher.title.singular") %></p>
  </div>

  <div class="grid grid-cols-2 gap-8 h-full">
    <%= render "form", field_service_group: @field_service_group, publisher: @publisher, field_service_report: @field_service_report %>
  </div>
</section>
