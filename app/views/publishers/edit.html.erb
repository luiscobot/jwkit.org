<% content_for(:title) { t("publisher.title.singular") } %>

<section class="flex flex-col gap-8 h-full">
  <div>
    <h1 class="font-inter font-bold text-xl leading-6 text-jw-white"><%= t("publisher.edit.title") %></h1>
    <p class="mt-2 font-inter font-medium text-base text-jw-quick-silver"><%= t("congregation") %> Rosales</p>
  </div>

  <div class="grid grid-cols-4">
    <%= render "form", publisher: @publisher, field_service_groups: @field_service_groups, commit: @commit %>
  </div>
</section>